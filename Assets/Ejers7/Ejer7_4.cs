using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Ejer7_4 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        for (int i = 100; i >= 1; i--)
        {
            Debug.Log(i); 
        }

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
